# 真实凭据，永不提交（见 计划.md §0）
# 本地模拟器 / ADB 端到端验收用；CI 单测用 app/src/test/resources/sample_import.txt
示例数据.md

# 本机临时产物：M0.5 协议踩点抓到的**原始**响应先落在这里（可能含密钥回显），
# 脱敏之后才复制成 app/src/test/resources/fixtures/ 下的 fixture。
.local/

# Gradle / Android
.gradle/
build/
local.properties
*.apk
*.aab
*.keystore
*.jks
captures/
.cxx/

# `gradle updateDaemonJvm` 生成的本机文件：里面把 daemon JVM 钉成 JDK 21，
# 与本项目声明的 JDK 17 不一致，且各平台的下载地址是逐机生成的，不入库。
gradle/gradle-daemon-jvm.properties

# IDE
.idea/
*.iml
.kotlin/

# OS
.DS_Store
Thumbs.db

# iOS / Xcode（iosApp 空壳工程，阶段4）
iosApp/build/
iosApp/iosApp.xcodeproj/xcuserdata/
iosApp/iosApp.xcodeproj/project.xcworkspace/xcuserdata/
*.ipa
*.xcarchive
DerivedData/

# WorkBuddy 项目数据（本地工作记忆，不入库）
.workbuddy/
