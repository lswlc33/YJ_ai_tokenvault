# 页面设计（不含设置页）

> 设置页沿用现有设计，不在本文档范围内。  
> 所有页面使用 MIUIX 风格组件：`AppScaffold` / `AppTopBar` / `AppCard` / `AppFilterChip` / `AppTextField` / `AppSecretTextField` / `AppDropdownRow` / `AppSwitchRow` / `AppActionRow` / `AppFab` / `AppBottomSheet` / `AppDialog` / `SectionTitle` / `EmptyState` / `StatusDot`。  
> 弹层一律使用 `Overlay*`，不使用 `Window*`。

## 0. 全局规则

### 组件与风格

- 每个页面自己套一层 `AppScaffold`，顶栏用 `AppTopBar`。
- 列表页用 `AppCard`，分组筛选用 `AppFilterChip`，不用固定 tab。
- 表单输入用 `AppTextField` / `AppSecretTextField`。
- 设置项用 `AppSwitchRow` / `AppDropdownRow` / `AppActionRow`。
- 状态展示用 `StatusDot` + 状态文字，不能只用颜色。
- 金额不做汇率换算，按币种并列展示。
- 时间统一显示相对时间，例如“更新于 3 分钟前”。

### 状态

每个页面都要覆盖：

- 加载中
- 空态
- 错误态
- 保存中
- 保存失败
- 网络失败

编辑页还需要：

- 未保存返回时弹确认框
- 保存中禁用保存按钮
- 保存失败保留用户输入

### 校验

- 供应商名称必填。
- 分组名不能为空，不能重复。
- Base URL 必填，不允许带 query string。
- 密钥值必填。
- 协议至少选择一个。
- 超时时间必须是数字，允许留空。

### 敏感信息

- API Key、账号密码默认遮蔽显示。
- 查看后自动回遮。
- 不把明文写进日志或异常消息。
- cURL 导入原文不进日志。

---

# 页面 - 仪表盘

## 顶栏

- 标题：仪表盘
- 右侧按钮：刷新

> 顶栏刷新只刷新当前展示数据，不触发全量探测。

## 内容区域

### 余额卡片

主题色大卡，按币种并列展示，不做汇率换算。

    USD 999.00
    CNY 666.00

    更新于 3 分钟前
    2 个供应商余额查询失败

规则：

- 没有配置余额查询时显示“未配置”。
- 查询失败时显示“查询失败”。
- 查询失败不在仪表盘展开明细，用户去管理页查看。
- 卡片右上角可放余额刷新按钮。

### 概览卡片

横向三个统计项：

    供应商    密钥    模型
      3        4       13

### 探测卡片

    探测

    供应商进度条
    共 1，成功 1，失败 0，跳过 0

    密钥进度条
    共 12，成功 11，失败 1，跳过 0

    上次更新：3 分钟前

规则：

- 显示最近一次探测结果。
- 失败数只做汇总，不提供单独明细页。
- 用户需要看具体失败项时，去管理页查看对应供应商和密钥。
- 探测进行中时显示当前进度。
- 从未探测过时显示“还没探测过”。

---

# 页面 - 管理

## 顶栏

- 标题：管理
- 右侧按钮：
  - 刷新
  - 编辑供应商列表

## 内容区域

### 搜索框

搜索供应商。

支持匹配：

- 名称
- 备注
- 官网
- 端点

### 分组

横向可滑动的 `AppFilterChip`，默认只有“全部”。

    全部    科研专用    GPT站点

规则：

- “全部”固定在第一位，不可编辑、不可删除。
- 其余分组由用户创建。
- 每个 chip 可显示该组供应商数量。

### 供应商列表

每张供应商卡片内直接展示该供应商下的密钥状态。

示例：

    Agent Router                  USD 761.97
    公司专用账号                  可用
    ps.air-outer.com              222ms
    Responses  Anthropic  Chat

    key1                          可用
    key2                          失败

示例 2：

    JustDoWork                    USD 761.97
    备用账号                      可用
    api.justworker.icu            222ms
    Anthropic Messages

    key1                          可用
    key2                          失败

卡片结构：

    供应商名称                    供应商余额
    供应商备注                    状态文字
    供应商官网                    延迟
    支持协议

    key1                          状态文字
    key2                          状态文字

布局规则：

- 备注和官网缺失时，剩余内容上下居中。
- 缺一个时，剩下两个居中。
- 状态必须用 `StatusDot` + 文字，不能只用颜色。
- 余额按币种展示，不做汇率换算。
- 卡片整体点击进入供应商详情页。
- 卡片长按进入多选模式。

### 批量操作

长按供应商卡片进入多选模式后：

- 顶栏显示已选数量。
- 支持全选 / 取消全选。
- 支持批量修改分组。
- 支持批量删除。
- 删除前弹确认框，说明会连带删除该供应商下的密钥、模型和账号。

### 悬浮按钮

`AppFab`

- 加号：添加供应商

> 不在管理页 FAB 中放导入入口。

### 空态

- 整库为空：提示“还没有供应商”，按钮为“添加供应商”。
- 搜索无结果：提示“没有匹配的供应商”。
- 当前分组为空：提示“这个分组下还没有供应商”。

---

# 页面 - 添加 / 编辑供应商

添加和编辑共用同一套页面结构，只区分标题和初始值。

## 顶栏

- 左侧按钮：返回
- 右侧按钮：保存

## 内容区域

### 基本信息

- 名称：必填
- 备注
- 官网

### 外观

- 分组：`AppDropdownRow`
- 颜色

> 协议不在这里配置。协议归密钥编辑页管理。

### 保存规则

- 名称为空时不能保存。
- 官网填了但格式不合法时提示错误。
- 有修改未保存时，返回需要确认。
- 保存成功后返回上一页。

---

# 页面 - 编辑供应商列表

这一页负责分组管理和供应商排序。

## 顶栏

- 左侧按钮：返回
- 右侧按钮：排序 / 保存

## 内容区域

### 分组设置

分组用 `AppCard` 展示。

    全部（固定分组）
    分组 1
    分组 2
    分组 3
    添加分组

规则：

- “全部”固定，不可编辑、不可删除、不可拖动。
- 点击已有分组弹出编辑对话框：
  - 名称文本框
  - 保存
  - 取消
- 点击“添加分组”弹出新建对话框：
  - 名称文本框
  - 保存
  - 取消
- 分组名不能为空，不能重复。
- 删除分组时，供应商回到“全部”，不删除供应商。

### 供应商排序

进入排序模式后，供应商卡片可拖动上下排序。

- 排序结果点“保存”后落库。
- 取消返回时不保存排序结果。

### 供应商分组

每个供应商一行，右侧用 `AppDropdownRow` 选择分组。

    供应商 1        分组 1
    供应商 2        分组 2
    供应商 3        全部

---

# 页面 - 供应商详情页

## 顶栏

- 标题：供应商名称
- 左侧按钮：返回
- 右侧按钮：
  - 编辑
  - 刷新

## 内容区域

### 头部卡片

主题色大卡。

    供应商名称
    供应商备注
    供应商官网
    支持协议
    状态文字
    延迟
    余额总计        USD 999.00
    上次更新        3 分钟前

规则：

- 余额按币种展示，不做汇率换算。
- 状态用 `StatusDot` + 文字。
- 官网可点击打开。
- 右上角可放余额刷新按钮。

### 密钥区

标题：密钥  
右侧按钮：添加

每把密钥一张 `AppCard`：

    我的密钥 1
    sk-12...da
    模型 1 个
    可用            USD 999.00
    上次更新        3 分钟前

规则：

- 点击密钥卡片进入密钥详情页。
- 密钥默认遮蔽显示。
- 状态用 `StatusDot` + 文字。
- 显示延迟和最近探测时间。
- 密钥按用户排序展示；余额查询使用排序第一的启用密钥。
- 删除密钥前弹确认框。

### 账号区

标题：账号  
右侧按钮：添加

每条账号一张 `AppCard`：

    账号 1
    GitHub  Linux DO  密码

规则：

- 点击账号卡片弹出编辑底部表单。
- 添加图标点击后也弹出同一个底部表单。
- 账号信息默认遮蔽显示。
- 删除账号前弹确认框。

### 添加密钥对话框

使用宽版 `OverlayDialog`。

    添加密钥

    cURL 导入
    手动导入

规则：

- 两个入口二选一。
- cURL 导入进入密钥 cURL 导入页。
- 手动导入进入密钥编辑页。

### 账号底部表单

使用 `AppBottomSheet`。

字段：

- 备注
- 登录方式：
  - GitHub 登录
  - Linux DO 登录
  - 密码登录

规则：

- 选择“密码登录”时展开账号和密码输入框。
- 保存后返回供应商详情页。

---

# 页面 - 密钥详情页

## 顶栏

- 标题：密钥名称
- 左侧按钮：返回
- 右侧按钮：
  - 编辑
  - 刷新

## 内容区域

### 密钥卡片

    密钥备注
    sk-12...da

    可用
    222ms
    上次更新：3 分钟前

    余额：USD 999.00

规则：

- 密钥默认遮蔽。
- 可点击“查看”临时显示明文。
- 查看后自动回遮。
- 状态用 `StatusDot` + 文字。
- 余额按币种展示。

### 连接卡片

    Base URL
    https://api.example.com

    协议：Responses  Chat  Anthropic Messages
    鉴权风格：Auto
    客户端预设：默认
    超时：35s
    允许 HTTP：关闭

### 模型列表

标题：模型  
右侧按钮：刷新

每个模型一行：

    模型 ID
    协议
    来源：手动 / 自动发现
    状态文字

规则：

- 点击模型行可查看模型详情。
- 长按模型行可快捷探测该模型。
- 模型列表为空时显示空态。
- 刷新失败时显示错误提示。

### 操作区

    编辑
    探测这一把密钥
    删除

规则：

- 删除前弹确认框。

---

# 页面 - 密钥 cURL 导入

## 顶栏

- 标题：密钥 cURL 导入
- 左侧按钮：返回
- 右侧按钮：导入

> 右侧按钮在识别成功前禁用。

## 内容区域

### 输入区

- 多行文本框
- 按钮：一键识别

规则：

- 一次只能粘贴一条 cURL。
- cURL 格式固定，解析结果不需要勾选。
- 解析失败时显示具体错误。
- 原文不进日志。

### 识别结果

识别成功后直接展示结果卡：

    Base URL
    https://api.example.com

    协议
    Responses

    API Key
    sk-12...da

    模型
    model-a

规则：

- 敏感字段默认遮蔽显示。
- 点击“导入”后写入当前供应商。
- 如果检测到已有相同 API Key：
  - 弹出提示：“这个密钥已经存在”
  - 用户可选择继续导入或取消
- 导入成功后进入密钥编辑页或返回供应商详情页。

---

# 页面 - 添加 / 编辑密钥

添加和编辑共用同一套页面结构，只区分标题和初始值。

## 顶栏

- 标题：添加密钥 / 编辑密钥
- 左侧按钮：返回
- 右侧按钮：保存

## 内容区域

### 基本信息

- 名称
- 备注
- API Key：`AppSecretTextField`

### 端点与协议

- Base URL：必填，不允许带 query string
- 协议：`AppFilterChip` 多选
  - Responses
  - Chat
  - Anthropic Messages

### 客户端

- 客户端预设：`AppDropdownRow`

### 高级设置

- Anthropic 路径覆盖
- 超时时间，单位秒
- 鉴权风格：`AppDropdownRow`
  - Auto
  - Authorization: Bearer
  - x-api-key
- 允许 HTTP：`AppSwitchRow`

### 模型列表

- 自动获取：`AppSwitchRow`

自动获取开启时：

- 不显示手动模型编辑区
- 显示最近同步时间和结果
- 支持立即同步按钮

自动获取关闭时：

- 显示手动模型列表
- 支持添加模型
- 支持删除模型

### 余额查询

- 余额查询：`AppSwitchRow`
- 查询方式：`AppDropdownRow`

根据下拉选择展示不同表单：

#### 不查询

不显示额外字段。

#### new-api / one-api

- 访问令牌
- 用户 ID

#### DeepSeek 官方

使用排序第一的启用密钥，不显示额外字段。

#### OpenRouter

使用排序第一的启用密钥，不显示额外字段。

#### SiliconFlow

使用排序第一的启用密钥，不显示额外字段。

#### Moonshot

使用排序第一的启用密钥，不显示额外字段。

#### 自定义 JSON

- 请求方法
- 请求路径
- 余额字段路径
- 已用字段路径
- 币种

### 探测设置

- 允许探测：`AppSwitchRow`
- 可达性检测：`AppSwitchRow`
- 密钥有效性检测：`AppSwitchRow`
- 余额查询：`AppSwitchRow`
- 模型列表同步：`AppSwitchRow`
- 模型测试：`AppSwitchRow`
  - 说明：允许发送真实请求测试模型可达性
- 快捷测试模型：`AppSwitchRow`
  - 说明：允许在密钥详情页的模型列表长按快速探测该模型

### 保存规则

- API Key 必填。
- Base URL 必填。
- 协议至少选择一个。
- 有修改未保存时，返回需要确认。
- 保存成功后返回上一页。


