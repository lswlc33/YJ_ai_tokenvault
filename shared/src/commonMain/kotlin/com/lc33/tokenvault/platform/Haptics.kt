package com.lc33.tokenvault.platform

/**
 * 震动/触觉反馈（红线 20：设备触觉是平台能力）。
 *
 * 只在需要的地方给「轻点」一级的触觉：底栏切 tab、开关切换、关键动作确认。
 * 不做持续震动（那不是触觉反馈，是闹铃）。
 */
expect object Haptics {
    /** 轻触（底栏切 tab、列表项点按这类高频交互）。强度最低、时长最短。 */
    fun tap()

    /** 标准冲击（开关切换、动作确认这类「状态真的变了」的反馈）。 */
    fun impact()
}
