# 迁移计划：Android + iOS 双端

> ## 阶段4 完成（2026-09-07 更新，待真机验收）
>
> 阶段 0–4 **全部完成并提交**：
> - 阶段1 换库降 KDF → 阶段2 shared 模块 + Koin/Ktor → 阶段3 全部 screens/ui/字符串迁
>   commonMain（Android 每阶段都独立构建全绿）；
> - 阶段4：Room 数据层（10 实体 / 72 查询 / 仓库 / 映射器）整体迁 commonMain（Room KMP，
>   schema 基线随迁）；Koin 拆成 coreModule/viewModelModule（跨平台）+
>   platformModule（expect/actual，Android/JVM/iOS 各一份）；iOS 全套平台件落地
>   （数据库 BundledSQLiteDriver / boot 存储 NSFileManager 原子写 / 剪贴板 UIPasteboard
>   localOnly）；`MainViewController()` 挂上真实 Compose 入口，Swift 壳与 IosBridge 退役。
> - **未签名 IPA 里已是完整应用**：图标、显示名、锁屏→引导→金库整棵树都在，
>   签名后可直接装。CI：macos-26 + 最新稳定版 Xcode 26（CMP 的 libCMPUIKitUtils.a
>   引用私有 UIUtilities 框架的 UIViewLayoutRegion，Xcode ≤16.4 的设备 SDK 没这个
>   符号）+ Gradle 堆 6g（K/N 链接整个 Compose UI + Room 的下限）。
>
> 还欠的事：
> - **真机验收**：签名安装后过一遍锁屏→引导→新建→导入→探测→备份；已知能力边界
>   ——前台空闲锁定与屏幕关闭即锁定在 iOS 上没有公开 API 可接，用进后台那条路兑底；
> - 迁移期间踩过的坑（jvmAndroid 源集让默认层级模板失效、ObjC 类属性/类方法的
>   Kotlin 形态、klib 编译其实 Windows 本机能跑）都写进了对应文件的注释与 CLAUDE.md。

## 目标与定位

| 项 | 决定 |
| --- | --- |
| 平台 | Android + iOS（不做桌面、Web） |
| 安全定位 | 记事本级。数据不重要，不要高强度加密，不要生物识别 |
| 加密 | 6 位数字 PIN → PBKDF2-HMAC-SHA256 → AES-256-GCM |
| 换设备 | 导出文件 → 新设备导入 → 输同样的 6 位数字 |
| 密码库 | BouncyCastle → `cryptography-kotlin`（跨平台，PBKDF2 与 AES-GCM 两端原生） |
| 构建 | 本机无 Mac，iOS 全部交给 GitHub Actions |
| 产物 | **未签名 ipa**，不做代码签名、不上架 |

## 阶段 0：前置

按 `new_plan.md` 把 Android 版写完（M8–M10）。这一步不做，后面全是空谈。

## 阶段 1：安全模型简化（可提前做，不改工程结构）

在现有 Android 工程内完成，改完 Android 端行为基本不变。

| 动作 | 范围 |
| --- | --- |
| 换密码库 + KDF 降级 | `crypto/` 14 文件 876 行 |
| 删 Keystore / BiometricPrompt / FLAG_SECURE | `platform/` 8 文件 1337 行 + `SecureFlag` 51 行 |
| 删除恢复密钥与独立备份口令 | 备份改用同一个 PIN，换设备靠「文件 + PIN」 |
| 保留 | `screens/lock/` 的 PIN 键盘（直接复用）、`DekEnvelope` 结构 |

结果：约删 2,000 行，`Argon2id` 与 BouncyCastle 从此消失。

## 阶段 2：抽 shared 模块（KMP 化，Android 照常跑）

新建 `:shared`，`commonMain` 收进 8 个**零 android 依赖**的包：
`domain` / `crypto` / `engine` / `probe` / `importer` / `balance` / `catalog` / `endpoint`

同步替换依赖：

| 现在 | 换成 | 量 |
| --- | --- | --- |
| Room 2.x | Room 3.0（`androidx.room3`，强制 suspend/Flow + 显式 SQLiteDriver） | 10 实体 / 72 查询 |
| Hilt | Koin 或 kotlin-inject | 107 处注解，`di/` 2 文件 358 行 |
| OkHttp | Ktor Client | `net/` 2 文件 225 行 |

UI 仍留在 `:app`，本阶段结束 Android 必须能独立构建。

## 阶段 3：UI 与资源搬家

- 485 个字符串键（521 处调用）从 `res/values/` 迁到 `composeResources`
- `screens/` + `ui/` 移入 `commonMain`：**`androidx.compose` 导入一行都不用改**
- 注意 `ArchitectureRulesTest` 的中文检查规则要跟着改
- 新增 `iosApp` Xcode 工程（KMP 模板自带的那种）

## 阶段 4：iOS 构建（GitHub Actions，不需要 Mac）

**前提：仓库是公开的**，GitHub Actions 对公开仓库完全免费，macOS runner 也一样。**这条是零成本的关键。**

### 4.1 编译验证 · 每次推送都跑

`runs-on: macos-15`（Apple Silicon，对应 `iosArm64` / `iosSimulatorArm64`）

- `:shared:compileKotlinIosArm64` — 验证代码能编过 iOS
- `:shared:iosSimulatorArm64Test` — 模拟器跑测试，不需要签名
- **必须缓存 `~/.konan` 与 Gradle 缓存**，否则每次二十分钟起步
- **这一步要尽早挂上**，用来最早暴露 `miuix-blur` 之类的 iOS 侧问题，别等写完才发现

### 4.2 未签名 ipa · 打 tag 时跑

Xcode 允许关掉签名直接构建：

```bash
xcodebuild archive -workspace iosApp.xcworkspace -scheme iosApp \
  -configuration Release -archivePath build/App.xcarchive \
  -destination generic/platform=iOS \
  CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO

mkdir -p Payload && cp -r build/App.xcarchive/Products/Applications/*.app Payload/
zip -r App-unsigned.ipa Payload
```

- 不需要 Apple 账号、不需要证书、不需要任何 secret
- 产物上传 artifact，免费下载

## 工作量

阶段 1 约 1 周；阶段 2–3 约 2–3 周；阶段 4（含调 CI）约 3–5 天。**（判断，非实测）**

执行顺序 0 → 1 → 2 → 3 → 4，**每阶段结束都要能独立构建**，不允许跨阶段堆积改动。
