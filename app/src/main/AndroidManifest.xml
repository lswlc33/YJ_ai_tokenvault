<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        allowBackup / fullBackupContent 关掉，dataExtractionRules 再显式排除
        databases/ 与 boot 文件：金库不允许被系统备份或设备迁移搬走（计划.md §7.5）。

        usesCleartextTraffic 是刻意开的：networkSecurityConfig 无法按用户输入动态放行，
        所以明文 HTTP 由应用层的 allowInsecure 开关管控（API 地址与 WebDAV 地址共用同一道闸），
        Manifest 只负责不提前掐死（计划.md §14.2、§15.12）。
    -->
    <application
        android:name=".TokenVaultApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TokenVault"
        android:usesCleartextTraffic="true">

        <!-- MainActivity 继承 FragmentActivity：便于 edge-to-edge 与生命周期回调（计划.md §15.1） -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- shared 侧平台能力（跳系统设置/打开链接/SAF 文件选择）自动注入 Application Context。 -->
        <provider
            android:name="com.lc33.tokenvault.platform.PlatformInitProvider"
            android:authorities="${applicationId}.platforminit"
            android:exported="false" />
    </application>

</manifest>
